spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/licoy/spring-cloud-study-config
          username:
          password:
#          basedir:
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/