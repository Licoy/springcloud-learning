spring:
  application:
    name: config-client
  cloud:
    config:
      # uri: http://localhost:8000/
      discovery:
        enabled: true
        service-id: config-server
      label: master
      profile: dev
server:
  port: 8001
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/